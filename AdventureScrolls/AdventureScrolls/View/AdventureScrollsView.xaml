<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModel="clr-namespace:AdventureScrolls.ViewModel"
             xmlns:local="clr-namespace:AdventureScrolls"
             x:Class="AdventureScrolls.View.AdventureScrollsView">
    <ContentPage.BindingContext>
        <viewModel:AdventureScrollsViewModel/>
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <ListView ItemsSource="{Binding Scrolls}" RowHeight="60">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <FlexLayout>
                            <Grid x:Name="MyGrid">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="30"/>
                                    <RowDefinition Height="50"/>
                                </Grid.RowDefinitions>
                                <Label Grid.Row="0" Padding="5" Text="{Binding EntryDate}"/>
                                <Label Grid.Row="1" Padding="5" Text="{Binding Title}"/>
                            </Grid>
                        </FlexLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
            
        </ListView>
    </ContentPage.Content>
</ContentPage>